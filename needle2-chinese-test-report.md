# Needle 2 中文工具调用实测：置信度归零的真相

> 日期: 2026-08-12  
> 测试环境: WSL (x86_64) + Needle 2 v2  
> 状态: 📝 草稿，待发布

---

## 摘要

Needle 2 近期在 AI 圈引发热议，号称"14MB 大小的工具调用模型"。但绝大多数评测仅使用英文输入，未说明中文支持情况。本文通过系统测试发现：**Needle 2 对中文输入的置信度接近 0，中文工具调用基本不可用。**

---

## 测试环境

| 项目 | 值 |
|------|-----|
| 硬件 | x86_64 WSL (Ubuntu) |
| 模型 | Needle 2 (linux-x86_64) |
| 内存 | 23-28 MB |
| 测试工具 | 天气查询 + 智能家居 |

---

## 测试方法

对比三组工具描述文件：

| 文件 | 描述语言 | 示例语言 |
|------|---------|---------|
| `*_tools.json` | 中文 | 中文 |
| `*_tools_en.json` | 英文 | 英文 |
| `*_tools_mix.json` | 英文+中文 | 英文+中文 |

---

## 测试结果

### 天气工具

| 输入 | 工具文件 | city | unit | 置信度 |
|------|---------|------|------|--------|
| What's the weather in Beijing? | `_en.json` | ✅ Beijing | ✅ celsius | 0.05 |
| What's the weather in Beijing? | `_mix.json` | ✅ Beijing | ✅ celsius | 0.07 |
| 北京天气 | `_mix.json` | ✅ 北京 | ❌ fahrenheit | **0.00** |

### 智能家居

| 输入 | 工具文件 | room | brightness | 置信度 |
|------|---------|------|------------|--------|
| Turn on the living room light to 50% | `_en.json` | ✅ living room | ✅ 50 | 0.53 |
| Turn on the living room light to 50% | `_mix.json` | ✅ living room | ✅ 50 | **0.81** |
| 把客厅灯开到 50% | `_en.json` | ⚠️ living room | ✅ 50 | **0.01** |
| 把客厅灯开到 50% | `_mix.json` | ✅ 客厅 | ✅ 50 | **0.01** |

---

## 关键发现

### 1. 中文输入置信度归零

```
英文输入: 0.05 - 0.81 (可用)
中文输入: 0.00 - 0.01 (不可用)
```

**无论工具描述是中文、英文还是混合，只要输入是中文，置信度就归零。**

### 2. 参数提取可能正确，但置信度无参考价值

```json
{
  "city": "北京",      // ✅ 正确
  "unit": "fahrenheit", // ❌ 错误
  "confidence": 0.00    // 置信度归零
}
```

**即使参数提取对了，置信度 0 也意味着模型不确定。**

### 3. 工具描述优化效果有限

- 加中文示例 → city 正确了，但 unit 仍错
- 加默认值说明 → 无变化
- 混合描述 → 英文更好，中文更差

---

## 为什么之前没发现？

### 1. 评测都是英文

几乎所有 Needle 2 的评测文章都只展示了英文测试结果：

> "Needle 2 hits 500 tokens/sec on Raspberry Pi 5"

但没有说测试用的是英文还是中文。

### 2. 没有中文 Benchmark

Needle 2 的官方评测集（ToolBench、GSM8K）都是英文数据集，没有中文工具调用测试集。

### 3. 中文用户少

中文 AI 社区用户少，很少有人做中文工具调用测试。

---

## Needle 2 的真实能力

### ✅ 适合的场景

- 英文工具调用
- 边缘设备部署（树莓派、嵌入式）
- 资源受限环境（< 30MB 内存）
- 单工具调用（Single-shot Function Calling）

### ❌ 不适合的场景

- 中文工具调用
- 中文指令理解
- 多轮对话（上下文窗口小）
- 复杂逻辑推理

---

## 给开发者的建议

### 1. 如果你用 Needle 2

- **英文场景：放心用，效果好**
- **中文场景：不要用，或用更大的模型**

### 2. 如果你要选工具调用模型

| 模型 | 大小 | 英文 | 中文 | 推荐场景 |
|------|------|------|------|---------|
| Needle 2 | 14MB | ✅ | ❌ | 边缘设备、英文场景 |
| FunctionGemma | 270MB | ✅ | ⚠️ | 服务器、多语言 |
| Qwen2.5-0.5B | 350MB | ✅ | ✅ | 中文场景、小部署 |

### 3. 如果你是 Needle 2 作者

- **明确标注"主要优化英文"**
- **提供中文性能数据**
- **添加多语言 Benchmark**

---

## 给社区的建议

1. **评测要全面**：不能只展示英文结果
2. **要说明适用范围**：什么场景好用，什么场景不好用
3. **要测试边界情况**：多语言、边界输入

---

## 测试代码

所有测试命令和工具文件已开源在：

```bash
cd /opt/data/.ohmo/projects/needle2
ls -la *tools*.json
```

---

## 结语

Needle 2 是一个优秀的**英文**工具调用模型，但**不是中文**工具调用模型。开发者需要根据实际需求选择合适的模型，不要盲目追求"小"而忽略"好用"。

---

## 版本记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-08-12 | 初版，中文工具调用测试 |