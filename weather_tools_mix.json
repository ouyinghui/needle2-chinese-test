[
  {
    "name": "get_weather",
    "description": "Get the current weather for a specified city. Example: 'weather in Beijing' -> city='Beijing', unit='celsius'. 中文示例: '北京天气' -> city='北京', unit='celsius'. Unless the user explicitly says 'Fahrenheit', unit is always 'celsius'.",
    "parameters": {
      "type": "object",
      "properties": {
        "city": {
          "type": "string",
          "description": "City name, e.g. 'Beijing', '上海'. Extract only the city name."
        },
        "unit": {
          "type": "string",
          "enum": ["celsius", "fahrenheit"],
          "description": "Temperature unit. Default is celsius. Only set to 'fahrenheit' if explicitly requested."
        }
      },
      "required": ["city"]
    }
  }
]
